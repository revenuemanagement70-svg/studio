{
  "entities": {
    "user": {
      "title": "User",
      "description": "A user of the application.",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The user's unique ID."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "displayName": {
          "type": "string",
          "description": "The user's display name."
        },
        "photoURL": {
          "type": "string",
          "format": "uri",
          "description": "The URL of the user's profile picture."
        }
      },
      "required": ["uid", "email"]
    },
    "hotel": {
      "title": "Hotel",
      "description": "A hotel property listed on the platform.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The unique ID of the hotel."
        },
        "name": {
          "type": "string",
          "description": "The name of the hotel."
        },
        "address": {
          "type": "string",
          "description": "The address of the hotel."
        },
        "price": {
          "type": "number",
          "description": "The price per night of the hotel."
        },
        "rating": {
          "type": "number",
          "description": "The rating of the hotel (out of 5)."
        },
        "amenities": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The list of amenities offered by the hotel."
        },
        "description": {
          "type": "string",
          "description": "A short description of the hotel."
        }
      },
      "required": ["id", "name", "address", "price", "rating", "description"]
    },
    "favoriteHotel": {
      "title": "Favorite Hotel Reference",
      "description": "A reference to a hotel that a user has saved as a favorite.",
      "type": "object",
      "properties": {
        "hotelId": {
          "type": "string",
          "description": "The ID of the favorited hotel document in the /hotels collection."
        }
      },
      "required": ["hotelId"]
    }
  },
  "auth": {
    "providers": ["anonymous", "google.com", "password"]
  },
  "firestore": {
    "/hotels/{hotelId}": {
      "schema": {
        "$ref": "#/entities/hotel"
      },
      "description": "A list of all hotel properties."
    },
    "/users/{userId}/favorites/{favoriteId}": {
      "schema": {
        "$ref": "#/entities/favoriteHotel"
      },
      "description": "A user's list of favorite hotel references."
    }
  }
}
