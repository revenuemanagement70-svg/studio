
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Helper functions
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return request.auth.uid == userId;
    }

    // HOTELS
    // Anyone can read hotel listings.
    // Only signed-in users can write (create, update, delete).
    // The complex validation function is removed to fix permission errors.
    match /hotels/{hotelId} {
      allow read: if true;
      allow create, update, delete: if isSignedIn();

      // Availability can be read by anyone, but only written by signed-in users.
      match /availability/{availabilityId} {
        allow read: if true;
        allow write: if isSignedIn();
      }
    }

    // USER DATA
    // Users can only access their own data (favorites).
    match /users/{userId} {
      // isUpdatingOwnData is removed for simplicity, can be re-added.
      allow read, write: if isOwner(userId);
      
      match /favorites/{favoriteId} {
        allow read, write: if isOwner(userId);
      }
    }

    // BOOKINGS
    // Only the user who created the booking can read it.
    // Signed-in users can create bookings (further validation is in the transaction).
    match /bookings/{bookingId} {
      allow read: if isSignedIn() && resource.data.userId == request.auth.uid;
      allow create: if isSignedIn();
      allow update, delete: if false; // Bookings are immutable by users.
    }
  }
}
